# Agent Instructions

## Commit Messages

All future commits must use the format `xxx(comp): desc`.

Examples:

- `feat(cli): add daemon health check`
- `fix(ci): stabilize smoke cleanup`
- `docs(agents): add commit message convention`
